env:
  - name: N8N_ENCRYPTION_KEY
    valueFrom :
      secretKeyRef:
        name: n8n-secrets # Nome do Secret que o Sealed Secrets irá criar
        key: encryption-key # Chave dentro do Secret
config:
  database:
    type: postgresdb
    postgresdb:
      host: 10.43.100.198
  generic:
    timezone: America/Sao_Paulo
  ssl_key:        # SSL Key for HTTPS Protocol - default: ''
  ssl_cert:       # SSL Cert for HTTPS Protocol - default: ''
secret:
  database:
    postgresdb:
      user: n8n
      password: '2LW9amUv' # Use Sealed Secrets para este valor também!
# Set additional environment variables on the Deployment
extraEnv:
  # Set this if running behind a reverse proxy and the external port is different from the port n8n runs on
  WEBHOOK_URL: 'https://n8n.julionogueira.net/'
  N8N_EDITOR_BASE_URL: 'https://n8n.julionogueira.net/'
  VUE_APP_URL_BASE_API: 'https://n8n.julionogueira.net/'
